# Diagnostic artifacts

The files in this directory preserve the analysis that exposed the inactive
shared-dealer mechanism. They are retained for traceability and are not final
thesis results. Original external paths and job identifiers inside JSON files
are provenance records rather than current repository paths.

See `inactive_dealer_audit.md` for the finding and corrective action.
