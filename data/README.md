# Empirical data placeholder

Raw and expanded Nasdaq TotalView--ITCH inputs are intentionally excluded from
version control. See the repository-level `DATA.md` for acquisition and
preprocessing guidance.

The small four-symbol configuration files under `config/` expect derived paths
such as `data/itch_20200130_qqq`. Populate those directories locally; do not
commit them.

