# Artificial input example

These files define four artificial book templates and the empirical-style
quantity, distance and background-rate distributions required by the legacy
background model. `--assets N` repeats the templates deterministically to
construct an `N`-book workload. The input contains no Nasdaq records.

The full 10,000-book command is shown in the repository README.
